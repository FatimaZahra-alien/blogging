/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://blogging-kappa-rosy.vercel.app', // <-- replace with your domain (or vercel URL for now)
  generateRobotsTxt: true, // generate robots.txt file
  sitemapSize: 7000,
};
